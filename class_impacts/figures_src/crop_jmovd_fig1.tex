% crop_jmovd_fig1 — temperature bins under uniform warming, lifted from the paper.
%
% Not a built figure: this is Figure 1 in Jones, Moscona, Olken & von Dessauer
% (2026), NBER w34671, page 39 of the PDF in ../papers/. It is a schematic of two
% hypothetical places rather than a plot of data, so there is nothing to rebuild
% from — the deck shows the paper's own graph with a source line.
%
% The trim values crop away the page margins, the "Figure 1:" caption, the notes
% block and the page number, keeping the plot, its axes and the legend. They are
% in points, measured from the edges of the 612 x 792 pt page. The include stays
% vector, so it stays sharp when projected.
%
% The image path is relative to the working directory, not to this file, so the
% build has to be run from class_impacts:
%
%   cd class_impacts
%   pdflatex figures_src/crop_jmovd_fig1.tex
%   mv crop_jmovd_fig1.pdf figures/fig_jmovd_uniformwarming.pdf
%
% Not part of the python figNN_ series, so build_all.py does not pick it up.
%
\documentclass[border=0pt]{standalone}
\usepackage{graphicx}
\begin{document}
\includegraphics[page=39, trim=76pt 334pt 24pt 226pt, clip]
                {../papers/w34671.pdf}
\end{document}
